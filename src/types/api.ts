export interface Experience {
  title: string;
  company: string;
  location: string;
  date: string;
  description: string[];
}

export interface Publication {
  title: string;
  conference: string;
  date: string;
  description: string;
}

export interface Project {
  title: string;
  stack: string;
  date: string;
  description: string[];
}